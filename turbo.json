{
  "$schema": "https://turbo.build/schema.json",
  "ui": "stream",
  "globalDependencies": ["tsconfig.base.json", ".env*"],
  "globalPassThroughEnv": [
    "DATABASE_URL",
    "REDIS_URL",
    "AUTH_SECRET",
    "AUTH_TOKEN_SECRET",
    "MFA_KEK_BASE64",
    "NEXTAUTH_URL",
    "NEXT_PUBLIC_SITE_URL",
    "NEXT_PUBLIC_API_URL",
    "NODE_ENV",
    "CI",
    "AWS_REGION",
    "AWS_S3_ENDPOINT",
    "AWS_S3_BUCKET_CONFORMITE",
    "AWS_S3_BUCKET_PROFILES",
    "AWS_KMS_PROFILES_KEY_ID",
    "CLOUDFRONT_PROFILES_DISTRIBUTION_ID",
    "CLOUDFRONT_PROFILES_PUBLIC_URL",
    "CV_SUGGESTED_COOKIE_SECRET",
    "CV_REVALIDATE_SECRET",
    "INTAKE_MAGIC_LINK_SECRET",
    "INTAKE_DISPOSABLE_EMAILS_REFRESH_INTERVAL_HOURS",
    "INTAKE_RATE_LIMIT_EMAIL_PER_24H",
    "INTAKE_RATE_LIMIT_IP_PER_24H",
    "INTAKE_BRIEF_EXPIRATION_DAYS",
    "SENTRY_DSN",
    "OTEL_EXPORTER_OTLP_ENDPOINT",
    "OTEL_EXPORTER_OTLP_HEADERS",
    "OTEL_SERVICE_NAME"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^build"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "typecheck": {
      "dependsOn": ["^build", "^typecheck"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "test:unit": {
      "outputs": ["coverage/**"]
    },
    "test:integration": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "test:e2e": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
