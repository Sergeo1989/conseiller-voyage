// Tailwind CSS v4 via le plugin PostCSS officiel (compatible Next 15 webpack + Turbopack).
// Tailwind v4 auto-détecte le contenu (scan du projet) — pas de `content` à déclarer.
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
