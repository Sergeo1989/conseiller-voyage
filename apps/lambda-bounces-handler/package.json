{
  "name": "@cv/lambda-bounces-handler",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "main": "./dist/handler.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-sns": "^3.0.0",
    "@types/aws-lambda": "^8.10.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0"
  }
}
